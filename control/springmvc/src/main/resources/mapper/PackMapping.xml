<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.springmvc.dao.PackDAO">
      <insert id="addPackInfo" parameterType="Package" >
      	 insert into package_info(p_sname,p_saddress,p_sphone,p_rname,p_raddress,p_rphone,p_type,p_weight,p_date,p_describ,p_number) 
      	 values (#{sname},#{saddress},#{sphone},#{rname},#{raddress},#{rphone},#{thingsType},#{weight},#{nowtime},#{describe},#{packnumber})
      </insert>
</mapper>
